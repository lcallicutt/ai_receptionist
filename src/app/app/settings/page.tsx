import { ComingSoon } from "@/components/portal/coming-soon";

export const metadata = { title: "Settings" };

export default function Page() {
  return (
    <ComingSoon
      title="Settings"
      description="Business profile, hours, holiday schedule, and service areas."
      phase="Phase 2"
    />
  );
}
