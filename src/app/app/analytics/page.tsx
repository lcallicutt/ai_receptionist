import { ComingSoon } from "@/components/portal/coming-soon";

export const metadata = { title: "Analytics" };

export default function Page() {
  return (
    <ComingSoon
      title="Analytics"
      description="Calls over time, conversion funnels, missed-call recovery, lead quality, and FAQ resolution rates."
      phase="Phase 8"
    />
  );
}
