import { ComingSoon } from "@/components/portal/coming-soon";

export const metadata = { title: "Team" };

export default function Page() {
  return (
    <ComingSoon
      title="Team"
      description="Invite teammates and manage roles — owner, manager, team member, and read-only."
      phase="Phase 2"
    />
  );
}
